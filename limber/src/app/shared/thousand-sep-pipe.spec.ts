import { ThousandSepPipe } from './thousand-sep-pipe';

describe('ThousandSepPipe', () => {
  it('create an instance', () => {
    const pipe = new ThousandSepPipe();
    expect(pipe).toBeTruthy();
  });
});
